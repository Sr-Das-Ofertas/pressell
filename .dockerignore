.git
.gitignore

# Node modules (serão instalados dentro dos containers)
node_modules/
backend/node_modules/

# Arquivos de ambiente (devem ser configurados na VPS, não commitados)
.env
backend/.env

# Lockfiles (serão gerados dentro do container ou copiados se existirem antes do build)
# bun.lockb # O Dockerfile do frontend copia este, então não ignorar se ele for commitado.
# backend/package-lock.json # O Dockerfile do backend copia este, então não ignorar.

# Logs e outros arquivos temporários
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos específicos do editor ou SO
.DS_Store
Thumbs.db
.vscode/ 